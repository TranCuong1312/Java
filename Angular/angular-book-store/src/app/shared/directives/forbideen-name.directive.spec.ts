import { ForbideenNameDirective } from './forbideen-name.directive';

describe('ForbideenNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbideenNameDirective();
    expect(directive).toBeTruthy();
  });
});
